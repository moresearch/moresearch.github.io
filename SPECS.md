# Post Specs

## User-facing behavior

- Each post source Markdown file in `posts/` becomes a rendered blog section inside the single-page site.
- Posts display a title, publication date, optional summary, and the rendered Markdown body.
- Posts appear newest first.

## Implementation rules

- Post source files must use the `.md` extension.
- Only files inside `posts/` are published as posts; this spec lives at the repo root (`SPECS.md`) so it can never be mistaken for a draft.
- Each post must start with front matter delimited by `---` lines.
- Required front matter fields are `title` and `date`. Keys are case-sensitive and must be lowercase.
- Optional front matter fields are `slug`, `summary`, and `tags`.
- `date` must use ISO-8601 calendar format (`YYYY-MM-DD`).
- The source `date` remains ISO-8601 in front matter even though the published site renders it as a larger Unix timestamp with a UTC hover tooltip.
- Front matter dates control the published chronology of the archive, so authors should set them deliberately.
- `slug`, when provided, must be URL-safe lowercase text using letters, numbers, and hyphens.
- If `slug` is omitted, the build derives it from the title.
- Markdown content begins after the closing front matter delimiter.
- Posts may cite external sources with standard Markdown links in the body, including direct links to arXiv paper pages.
- Posts may also cite workshops, talks, videos, and companion repositories when a reflection post is grounded in their technical content.
- Posts that cite YouTube videos should include a linked thumbnail image near the relevant discussion rather than a text-only reference. Use the YouTube thumbnail URL format `https://img.youtube.com/vi/<VIDEO_ID>/hqdefault.jpg` and wrap the image in a Markdown link to the video. When referencing a specific scene, include a timestamp (MM:SS) or range (MM:SS–MM:SS) and link to the video with that timestamp.
- Posts may embed remote publisher-hosted book-cover images when discussing influential books tied directly to the post topic.
- Posts may compare concrete Go libraries or frameworks when the comparison is grounded in current official docs and includes code examples.
- Posts may connect personal skill development to games or other systems when the essay stays specific about the engineering habits or mental models that transferred.
- Fenced code blocks with language info strings are allowed and should be used when posts include code examples.
- Highlighted code blocks inherit the shared site-wide syntax theme automatically; authors should only supply the correct language info string.
- When a post includes code, the shared snippet styling should render it in a monochrome `vim-256noir`-style palette and Sudo font without post-level overrides.
- Go code examples should include brief comments so the example remains self-explanatory inside the single-page archive.
- Code-block scrollbars should only become visibly styled when the reader hovers the block.
- Markdown links inside post bodies are rendered to open in a separate browser tab/window by default.
- Rendered post bodies use the site's configured monospace body font; post authors should not rely on custom font styling in Markdown.
- Rendered post-body links follow the site theme and should not rely on underline styling for visibility.
- Each post slug also drives the in-page navigation state, so slugs must stay stable and unique.
- Post length may vary, including medium-length essays, as long as the Markdown stays readable in the single-page archive.
- Reflection posts should explain what specifically was learned, what tradeoffs mattered, and why those details changed the author's view of the topic.

## Constraints and invariants

- Two posts must never resolve to the same slug.
- Draft handling: Posts may include optional front matter `draft: true` to exclude them from the published archive. When consolidating posts, create a canonical merged post (with a stable slug) and mark original files with `draft: true` and a short note linking to the canonical post. The generator will skip files marked `draft: true`. 
- Post bodies may include standard Markdown links, lists, headings, blockquotes, and fenced code blocks.

## Change log:
- Added post 2026-05-17-task-harness-engineering.md: "Task Harness Engineering" (source: YouTube C_GG5g38vLU).
- Added post 2026-05-17-harness-engineering-fowler.md: "Harness Engineering (Martin Fowler)" (source: https://martinfowler.com/articles/harness-engineering.html).
- Added post 2026-06-06-ai-sovereignty-or-ai-colony.md: "AI sovereignty or AI colony: why domestic capability matters" (source: https://thenextweb.com/news/japan-risks-becoming-an-ai-colony-its-digital-minister-warns).
- Updated post 2026-06-06-ai-sovereignty-or-ai-colony.md to include the requested article photo: https://media.thenextweb.com/2026/06/Hisashi-Matsumoto.avif.
- Added post 2026-09-23-llm-at-runtime-is-an-antipattern.md: "LLM at Runtime Is an Antipattern" — an LLM in the event loop converts a fixed cost into a variable cost and gives up replayability, auditability, versioning, and determinism; the fix is a hard wall between compile time (LLM as the semantic layer of AutoML, proposing candidate features) and runtime (frozen, hash-bound artifacts plus a small traditional model). Discusses TypeSafe's System One / Jev launch post (https://typesafe.ai/blog/introducing-system-one-models-and-jev) in detail, including three linked figures from it (the published decision DAG, the workflow-eval Pareto plot, and the wrong-tool-call / type-error evaluation, hosted at framerusercontent.com), plus Laya (https://huggingface.co/convaiinnovations/laya) as the open-weight counterpart. References include Brooks (*No Silver Bullet*, https://www.cs.unc.edu/techreports/86-020.pdf), Boehm & Basili (*Software Defect Reduction Top 10 List*, https://web.archive.org/web/2020/https://www.cs.umd.edu/~basili/publications/journals/J81.pdf), CAAFE (https://arxiv.org/abs/2305.03403), Shwartz-Ziv & Armon (https://arxiv.org/abs/2106.03253), Grinsztajn et al. (https://arxiv.org/abs/2207.08815), Sculley et al. hidden technical debt (https://papers.nips.cc/paper/5656-hidden-technical-debt-in-machine-learning-systems.pdf), and internal post cross-links.
- Updated post 2026-09-23-llm-at-runtime-is-an-antipattern.md: tightened for coherence and focus, from 18 sections to 11 (merged the pattern section with the marginal-cost argument, the two decision-model sections into "The Waypoint", the pipeline and hash sections into "The Wall", and "The Point" with "A Call to the Community"), dropped the standalone "Broader Trend" list into a closing paragraph, and cut ~1,500 words of main body plus a handful of orphaned references; the three TypeSafe figures, the Doom/Jevons economics, the Laya ceilings, and the reference apparatus are unchanged.
- Updated post 2026-09-23-llm-at-runtime-is-an-antipattern.md: restored *A Call to the Community* as its own closing section (12 sections total) with all six asks — the earlier tightening pass had folded it into "The Point", which now ends at the architecture summary so the post closes on the call.
- Updated post 2026-09-23-llm-at-runtime-is-an-antipattern.md: prose pass plus a new section — *The DSPy Lesson: Compile the Program, Not the Prompt* (declarative signatures, composable modules, metric-driven compilation, shipped compiled artifacts; the argument keeps the principles and changes the compiled artifact from a prompt bundle to `spec.yaml` + `model.cbm` + hashes). DSPy is also woven into *The Wall* and promoted into the call to the community as its own ask. References gain a *Compound AI systems, DSPy, and compiled programs* group (BAIR compound-AI framing, arXiv:2310.03714, dspy.ai docs, MIPROv2 arXiv:2406.11695, GEPA arXiv:2507.19457). Stiff opening/closing lines rewritten.
- Updated post 2026-09-23-llm-at-runtime-is-an-antipattern.md: added *The Better Teacher: TabPFN, Not the LLM* — knowledge distillation with a tabular foundation model as the compile-time teacher and CatBoost (CrossEntropy objective) as the frozen student, with the four reasons a tabular teacher beats an LLM teacher (tabular pretraining and its benchmark numbers, discontinuous numeral tokenisation plus serialisation/column-order sensitivity, in-context row capacity, checkpoint size versus API cost), the honest caveats (licence split between TabPFN-2 and the non-commercial 2.5/2.6/3/3.5 weights, moving row/column caps and context sampling, teachers that cannot teach what they do not know), and the division of labour — LLM for meaning, tabular model for statistics. Woven into *When the Wall Is Wrong* (prefer the tabular labeler when the cold-start table is structured) and into the call to the community as part of the *measure your traditional models* ask. References gain a *Tabular foundation models as teachers* group (arXiv:2207.01848 and the Nature paper, the TabPFN-2.5/3/3.5 reports, Prior Labs model/licence docs, Hinton distillation, PFN-Boost, TabDistill, in-context data distillation, Tab-AICL and the VBLL calibration comparison, CatBoost objectives, and the LLM-teacher side: TabLLM, structural priors, xVal).
- Updated post 2026-09-23-llm-at-runtime-is-an-antipattern.md: added the *Pocket Foundation Models* reference (arXiv:2605.18654, https://arxiv.org/pdf/2605.18654) and rebuilt the teacher section around it. The recipe's code block now uses stratified out-of-fold teacher labelling, because an in-context teacher that scores rows inside its own context leaks labels and collapses its soft targets to a near-one-hot distribution (mean entropy ~10⁻³ nats versus 0.6–0.9 out of context) — and the paper reports that skipping OOF makes distilled students worse than hard-label training. Turned the comparison into an explicit *why an LLM is not suited to teaching CatBoost* list (its targets are either leaked or prompt-shaped rather than a posterior, it must read numbers as text, it cannot see the whole table, the student inherits the teacher's rank so a sub-CatBoost LLM teacher ships a worse model, and its targets cannot be regenerated or hashed), with the paper's measurements (96.5% of teacher AUC at 1.9 ms CPU, 38–860x, Wilcoxon p=0.0008 over tuned CatBoost; teacher selection is a one-decision problem; gains concentrate at ≤21 features; multi-teacher averaging is negligible for trees) and its own limits (single seed, IID-per-dataset so temporal shift is untested, per-core latency). Reference group gains the paper, TabTune, TabICLv2, and LimiX.
- Updated post 2026-09-23-llm-at-runtime-is-an-antipattern.md: added *The Jevons Trap* (15 sections total), a proper treatment of the rebound effect placed between the decision-model waypoint and the AutoML section: Jevons' 1865 coal observation, the mechanism (efficiency lowers the effective price, elastic demand raises total consumption, so elasticity decides rather than efficiency), the four AI-specific strands (Luccioni/Strubell/Crawford on rebound undermining efficiency-only arguments; Zhang & Zhang's structural Jevons paradox from super-elastic compute demand; Sharma's thermodynamic cloud model locating system growth as the driver; Husom et al. on prevention over efficiency), TypeSafe's own public confirmation ($0.042/MTok pricing, the ~$7/hour Doom demo reported as lower than expected), and the architectural conclusion — efficiency is not a strategy, decoupling is, with the honest residual that the trap is relocated to the number of decisions you choose to model rather than the number of events customers send. The Waypoint's Jevons paragraph was reduced to the naming observation plus a pointer so the argument is made once, the unit-economics ask in the call now carries the Jevons clause, and the summary/tags (jevons-paradox, rebound-effect) plus a seven-entry reference block were added.
- Added post 2026-09-23-the-brain-is-a-key-not-a-box.md: "The Brain Is a Key, Not a Box" — the storage metaphor of memory (encode, compress, file, retrieve, unzip) examined against Victoria Trumbull's *Memories are not stored in the brain* (IAI; linked thumbnail in the talk's own og:image) and then carried into AI: the three failures (reconsolidation rewrites on read, engrams are distributed rather than located, the store is never powered off), the Bergsonian and block-universe readings of "time stores memory", the honest objections (eternalism is one live interpretation, and you still need a casual key), then the machine half — symbolic AI as a database versus weights as enabling structure, parametric vs non-parametric memory, why knowledge editing is hard (ROME), hallucination as confabulation, recurrence as duration (with the fixed-width-state disanalogy), attention/context and the retrieval turn (RAG, kNN-LM, RETRO, Memorizing Transformers, Lost in the Middle), and a case study on gradient boosting: naive mean encoding as the zip-file move, CatBoost's own phrase for its ordered target statistics ("an artificial 'time'"), the `has_time` switch that separates *an* order from *your* order, oblivious trees, and XGBoost's native categorical support since 1.5. References: 52 links (all verified), including SEP entries, Semon's *Mneme*, Bergson, Whitehead, Sider, Canales, Rovelli, Clark & Chalmers, Varela et al., James's transmission theory, and the ML papers.
- Updated post 2026-09-23-the-brain-is-a-key-not-a-box.md: tightened for length — main body cut from ~4,700 to ~3,640 words and the reference apparatus from ~1,300 to ~920, mostly by compressing the three failures to one paragraph each, trimming the block-universe and physics-of-time paragraphs, shortening the machine half (parametric memory, ROME, hallucination) and the case study (XGBoost paragraph, correction paragraph), folding the fourth "what changed" item into the third, and merging six reference bullets into four. All arguments, evidence, quotes (including the *artificial time* line from the CatBoost paper and the `has_time` documentation), figures, code blocks, and links are retained; the site was regenerated and all 51 links re-verified.
